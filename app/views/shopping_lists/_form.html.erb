<%= form_with(model: shopping_list, local: true) do |f| %>
  <% if shopping_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shopping_list.errors.count, "error") %> prohibited this shopping_list from being saved:</h2>

      <ul>
      <% shopping_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Shopping List Name" %>
    <%= f.text_field :name %>
  </div>

  <%= f.fields_for :products do |builder| %>
    <%= render 'product_fields', f: builder %>
  <% end %>

  <div>
  <%= link_to_add_product "Add Product", f, :products%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
